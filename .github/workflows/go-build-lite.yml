name: Go-Build-Lite

on:
  push:
    branches:
      - "lite"
    paths:
      - .github/workflows/**
      - cmd/**
      - internal/**
      - go.mod
  pull_request:
    branches:
      - "lite"
  workflow_dispatch:


jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.23.4'

      - name: Build
        run: go build -o fsd-linux-amd64-lite-${{ github.sha }} ./cmd/fsd/

      - name: Run UPX
        uses: crazy-max/ghaction-upx@v3
        with:
          version: latest
          install-only: true

      - name: Compress executable file
        run: upx -fq -9 ./fsd-linux-amd64-lite-${{ github.sha }}

      - uses: actions/upload-artifact@v4
        with:
          name: linux-nightly
          path: ./fsd-linux-amd64-lite-${{ github.sha }}

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.23.4'

      - name: Build
        run: go build -o fsd-windows-amd64-lite-${{ github.sha }}.exe ./cmd/fsd/

      - name: Run UPX
        uses: crazy-max/ghaction-upx@v3
        with:
          version: latest
          install-only: true

      - name: Compress executable file
        run: upx -fq -9 ./fsd-windows-amd64-lite-${{ github.sha }}.exe

      - uses: actions/upload-artifact@v4
        with:
          name: windows-nightly
          path: ./fsd-windows-amd64-lite-${{ github.sha }}.exe